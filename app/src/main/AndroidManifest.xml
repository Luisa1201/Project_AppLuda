<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================ -->
    <!-- PERMISOS                                     -->
    <!-- ============================================ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_r"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_r_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- ============================================ -->
        <!-- WELCOME ACTIVITY - Pantalla de inicio       -->
        <!-- ============================================ -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============================================ -->
        <!-- LOGIN ACTIVITY - Pantalla de inicio sesión  -->
        <!-- ============================================ -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />

        <!-- ============================================ -->
        <!-- REGISTER ACTIVITY - Pantalla de registro    -->
        <!-- ============================================ -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            tools:ignore="LockedOrientationActivity" />

        <!-- ============================================ -->
        <!-- HOME ACTIVITY - Pantalla principal          -->
        <!-- ============================================ -->
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            tools:ignore="LockedOrientationActivity" />

        <!-- ============================================ -->
        <!-- SUPPORT ACTIVITY - Pantalla de soporte      -->
        <!-- ============================================ -->
        <activity
            android:name=".SupportActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity"
            tools:ignore="LockedOrientationActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".HomeActivity" />
        </activity>

        <!-- ============================================ -->
        <!-- VIDEO PLAYER ACTIVITY - Reproductor         -->
        <!-- ============================================ -->
        <activity
            android:name=".VideoPlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            tools:ignore="LockedOrientationActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SupportActivity" />
        </activity>

    </application>

</manifest>